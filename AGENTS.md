# Product conventions

- Keep product tools off the primary navigation. List them on `/tools/`, and link that directory from the footer's Explore section.
- Give every tool its own dedicated route. End each tool page with `Built with 💚 by codeasystem.com`.
- Tool interfaces should prioritise a simple, focused workflow over feature density.
